name: Deploy Vue

on:
  push:
    branches: [main] # Ensure this is 'main' or 'master' depending on your repo

permissions:
  contents: read
  pages: write
  id-token: write

# Prevent multiple deployments from running at once
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: npm install

      - name: Fix Permissions
        # This solves the 'vite: Permission denied' error
        run: chmod -R +x node_modules/.bin

      - name: Build
        # This will now have permission to run
        run: npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4